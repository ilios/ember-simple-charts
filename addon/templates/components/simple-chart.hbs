{{component chartName
  data=data
  width=svgWidth
  height=svgHeight
  isIcon=isIcon
  hover=(action 'hover')
  click=(action 'click')
  leave=(action 'leave')
}}

{{yield (hash
  tooltip=(component 'simple-chart-tooltip' slice=tooltipSlice location=tooltipLocation)
)}}
