<div
  class="simple-chart"
  {{did-resize this.calculateSize}}
  {{did-insert this.calculateSize}}
>
  {{#let (component this.chartName) as |Chart|}}
    <Chart
      @data={{@data}}
      @isIcon={{this.isIcon}}
      @hover={{perform this.handleHover}}
      @click={{perform this.handleClick}}
      @leave={{perform this.handleLeave}}
      @isClickable={{this.isClickable}}
      @containerHeight={{this.height}}
      @containerWidth={{this.width}}
    />
  {{/let}}
  {{#if this.tooltipTarget}}
    {{yield
      (hash
        tooltip=(component "simple-chart-tooltip" target=this.tooltipTarget title=null)
      )
    }}
  {{/if}}
</div>
